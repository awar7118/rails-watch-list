<h1>Your List Details..👇🏾</h1>

<%= @list.name %>
<br>
<br>
<ul>
<% @list.bookmarks.each do |bookmark| %>
<li><%= bookmark.movie.title %> <%= link_to 'Delete', bookmark_path(bookmark), method: :delete, class: "btn btn-danger mt-1", data: { confirm: 'Are you sure?' } %>
</li>
<% end %>
</ul>
<br>
<%= link_to 'create a new bookmark', new_list_bookmark_path(@list), class: "btn btn-success mt-3"%>
<%= link_to 'Go back', lists_path, class: "btn btn-success mt-3" %>
<%# <%= link_to 'Delete bookmark', bookmark_path, method: :delete, data: { confirm: 'are you sure?' },class: "btn btn-danger mt-3" %>
